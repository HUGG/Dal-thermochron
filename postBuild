# Clone tcplotter
git clone https://github.com/HUGG/tcplotter.git
cd tcplotter
python -m pip install -e .

# Compile codes for (U-Th)/He and FT age prediction
cd src/ketch_aft
make && make install
cd ../RDAAM_He
make && make install
cd ../..

# Return to base directory
cd ..